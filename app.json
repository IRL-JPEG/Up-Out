{
  "name": "up-and-out",
  "description": "A portrait glass-elevator adventure with 45 illustrated rooms, photo favours and rhyming character verdicts",
  "env": {
    "REACTOR_API_KEY": {
      "description": "rk_ key from reactor.inc dashboard. Never shipped to the browser.",
      "required": false
    },
    "ANTHROPIC_API_KEY": {
      "description": "For floor-specific photo observations and JSON grades",
      "required": false
    },
    "MOCK_AI": {
      "description": "1 forces an explicitly labelled illustrated preview",
      "value": "0",
      "required": false
    },
    "DEMO_MODE": {
      "description": "1 enables the clearly labelled photo skip for demonstrations",
      "value": "0",
      "required": false
    },
    "ELEVENLABS_API_KEY": {
      "description": "ElevenLabs server API key",
      "required": false
    },
    "ELEVENLABS_VOICE_ID": {
      "description": "Default factory-worker voice for requests, rhyming replies and keepsakes",
      "value": "NbvR1eY6Q8ivACdEO8PV",
      "required": false
    },
    "ELEVENLABS_AGENT_ID": {
      "description": "Optional factory-worker conversation agent",
      "required": false
    }
  }
}
