{
  "name": "up-and-out-web",
  "version": "0.1.0",
  "private": true,
  "description": "Up and Out: lift panel, world-model floors on Reactor, Storeroom 54 fetch quest",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "MOCK_AI=1 node server/index.js",
    "check": "node --check server/index.js && node --check server/grader.js && node --check public/js/content.js && node --check public/js/audio.js && node --check public/js/worlds.js && node --check public/js/spatial.js && node --check public/js/app.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
