Drop `intro.mp4` here (portrait, muted, ends on the panel frame). The page plays it on first tap and the
interactive panel appears on its last frame. Without it the page zooms into the panel image instead.
